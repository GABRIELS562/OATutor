apiVersion: v1
kind: Service
metadata:
  name: {{ include "online-tutor.fullname" . }}
  labels:
    {{- include "online-tutor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "online-tutor.selectorLabels" . | nindent 4 }}
